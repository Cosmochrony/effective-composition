# Direct dependencies pinned for figure generation (numerical scripts are stdlib-only and need no install).
# Transitive dependencies are not locked; the figure checksum in ARTIFACT_SHA256SUMS is the acceptance test.
# Generated and verified under CPython 3.14.5; any CPython >= 3.9 reproduces the exact rational outputs.
matplotlib==3.10.8
numpy==2.4.6
